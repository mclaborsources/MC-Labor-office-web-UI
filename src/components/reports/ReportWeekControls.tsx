"use client";
import { thisWeekDayDate } from "@/lib/week";
export function initialReportDate(){const d=thisWeekDayDate(new Date(),7);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;}
export function ReportWeekControls({date,onChange}:{date:string;onChange:(date:string)=>void}){const ending=new Date(date+"T12:00:00");const start=new Date(ending);start.setDate(start.getDate()-6);const payday=new Date(ending);payday.setDate(payday.getDate()+6);function move(days:number){const d=new Date(ending);d.setDate(d.getDate()+days);onChange(`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`);}const long=(d:Date)=>d.toLocaleDateString("en-US",{weekday:"long",year:"numeric",month:"long",day:"numeric"});return <div className="report-week"><label>Week Ending Friday<input type="date" value={date} onChange={e=>{if(e.target.value){const d=thisWeekDayDate(new Date(e.target.value+"T12:00:00"),7);onChange(`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`);}}}/></label><div><i>(click date first)</i><div><button aria-label="Previous week" onClick={()=>move(-7)}>⬅</button><button aria-label="Current week" onClick={()=>onChange(initialReportDate())}>▣</button><button aria-label="Next week" onClick={()=>move(7)}>➡</button></div></div><label>Start and End Days of Report<output>{long(start)}</output><output>{long(ending)}</output></label><label>Payroll Chk Day<output>{long(payday)}</output></label></div>;}
