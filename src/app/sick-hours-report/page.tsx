import{AppShell}from"@/components/layout/AppShell";import{OperationalReportScreen}from"@/components/reports/OperationalReportScreen";import{getSessionOrDefault}from"@/lib/auth/session";import{getSickHoursRows,type OperationalReportRow}from"@/lib/operationalReports";
export default async function Page(){const session=await getSessionOrDefault();const year=new Date().getFullYear();let rows:OperationalReportRow[]=[];let error="";try{rows=await getSickHoursRows(year)}catch{error="Sick-hours records could not be loaded from SQL Server."}return <AppShell userDisplayName={session.user?.displayName} fullWidth fillViewport><OperationalReportScreen title="Sick Hours Report - All" variant="sick" rows={rows} error={error}/></AppShell>}
