import {AppShell} from "@/components/layout/AppShell";import {OperationalReportScreen} from "@/components/reports/OperationalReportScreen";import {getSessionOrDefault} from "@/lib/auth/session";import {getNoticeOfIdentificationRows,type OperationalReportRow} from "@/lib/operationalReports";
export default async function Page(){const session=await getSessionOrDefault();let rows:OperationalReportRow[]=[];let error="";try{rows=await getNoticeOfIdentificationRows();}catch{error="Notice of Identification records could not be loaded from SQL Server.";}return <AppShell userDisplayName={session.user?.displayName} fullWidth fillViewport><OperationalReportScreen title="Notice of Identification Search" variant="noi" rows={rows} error={error}/></AppShell>}
