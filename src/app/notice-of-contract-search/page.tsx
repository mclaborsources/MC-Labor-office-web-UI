import {AppShell} from "@/components/layout/AppShell";import {OperationalReportScreen} from "@/components/reports/OperationalReportScreen";import {getSessionOrDefault} from "@/lib/auth/session";import {getNoticeOfContractRows,type OperationalReportRow} from "@/lib/operationalReports";
export default async function Page(){const session=await getSessionOrDefault();let rows:OperationalReportRow[]=[];let error="";try{rows=await getNoticeOfContractRows();}catch{error="Notice of Contract records could not be loaded from SQL Server.";}return <AppShell userDisplayName={session.user?.displayName} fullWidth fillViewport><OperationalReportScreen title="Notice of Contract Search" variant="noc" rows={rows} error={error}/></AppShell>}
